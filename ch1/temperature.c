#include <stdio.h>

float celcius(float far);

/* print the Farenheight to Celcius scale */
int main()
{
  printf("f:%6.2f\n", celcius(10.0));
  return 0;
}

/* given a farenheight float retunr the celcius value */
float celcius(float far)
{
  return 10.0;
}

